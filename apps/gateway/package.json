{
  "name": "@opencode-ui/gateway",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest",
    "test:run": "vitest run",
    "lint": "tsc --noEmit -p tsconfig.json",
    "check": "npm run typecheck && npm run test:run && npm run build"
  },
  "dependencies": {
    "fastify": "^5.0.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "tsx": "^4.19.0",
    "vitest": "^4.1.0",
    "@types/node": "^22.0.0"
  }
}
