# Phase 1A Frontend Shell — No environment variables required.
#
# This is a pure frontend application. No API keys, backend URLs,
# or runtime secrets are needed for development or builds.
#
# Environment variables will be defined in later phases when
# backend integration (Fastify gateway, SSE, etc.) is implemented.
